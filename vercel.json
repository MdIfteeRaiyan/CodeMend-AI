{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "corepack pnpm install --frozen-lockfile",
  "buildCommand": "corepack pnpm build:client",
  "outputDirectory": "dist/public",
  "functions": {
    "api/execute.ts": { "maxDuration": 30 }
  }
}
